.DS_Store
.venv/
.pytest_cache/
__pycache__/
*.pyc
raw_data/
data/
*.db
*.sqlite
*.sqlite3

.coverage
.claude/
results/
htmlcov/
features_vlm/

apps_sdk/web/node_modules/
apps_sdk/web/dist/

# AWS credentials / secrets — never commit
aws_credentials.sh
aws_dataset_access.sh
.env
.env.*
!.env.example
downloads/

# Offline artifacts live in the docker volume, not the repo — ignore if ever
# copied out to the worktree (e.g. by `docker compose cp`).
/data/
*.pkl
listings.db

# Aulia's test helper + general log rule
test_hard_facts.sh
logs/

# Local Hugging Face cache (multi-GB; set HF_HOME here in dev)
.hf_cache/

# Cursor IDE local state (optional: keep .cursor/rules/ tracked by removing this)
.cursor/
